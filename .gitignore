# Go
/bin/
*.exe
*.test
*.out
/vendor/

# Host keys & secrets — never commit
*.pem
*.key
id_ed25519*
host_key*
host_ed25519*
/.ssh/
config.yaml
.env
.env.*

# Production notebook content lives on the server volume / a separate repo.
# The committed ./notebook is only sample content for local dev.
/content/

# OS / editor
.DS_Store
*.swp
